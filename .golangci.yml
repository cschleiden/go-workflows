run:
  tests: false

linters:
  enable:
  - goworkflows

linters-settings:
  custom:
    goworkflows:
      path: ./plugin.so
      description: go-workflows
      original-url: github.com/ticctech/go-workflows/analyzer